{
  "name": "@rotundasoftware/baseline",
  "version": "11.0.0",
  "description": "Share code and data between the client and the server in full stack JavaScript applications.",
  "exports": {
    ".": "./lib/baseline.js",
    "./baselineService": "./lib/baselineService.js",
    "./collectionService": "./lib/baselineService.js"
  },
  "main": "",
  "scripts": {
    "prepublishOnly": "npm install --only=dev && babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "eslint": "eslint --ext '.es' .",
    "eslint:strict": "eslint --ext '.es' . --max-warnings=0",
    "prepush": "npm run eslint:strict"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "chai": "^4.2.0",
    "mocha": "^6.2.0"
  },
  "dependencies": {
    "backbone-events-standalone": "^0.2.1",
    "class-con-leche": "^0.0.0",
    "es6-promise": "^4.1.1",
    "jquery": "^3.5.1",
    "matches-where-query": "^1.0.0",
    "node-uuid": "^1.4.1",
    "steamer": "^1.3.0",
    "underscore": "^1.13.2"
  },
  "author": "Rotunda Software",
  "license": "MIT"
}
